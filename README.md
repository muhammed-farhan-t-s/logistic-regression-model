# logistic-regression-model
A logistic regression model is a statistical and machine learning algorithm that predicts the probability of a binary outcome (e.g., yes/no, 0/1) using an S-shaped curve, also known as the sigmoid function
